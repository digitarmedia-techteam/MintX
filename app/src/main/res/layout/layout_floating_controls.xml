<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="wrap_content"
    android:layout_height="wrap_content"
    android:padding="16dp"
    android:clipToPadding="false">

    <!-- Hint Button (Floating to the left of timer) -->
    <com.google.android.material.card.MaterialCardView
        android:id="@+id/card_hint"
        android:layout_width="48dp"
        android:layout_height="48dp"
        android:layout_marginEnd="16dp"
        app:cardBackgroundColor="@color/white"
        app:cardCornerRadius="24dp"
        app:cardElevation="6dp"
        app:layout_constraintBottom_toBottomOf="@id/card_floating_timer"
        app:layout_constraintEnd_toStartOf="@id/card_floating_timer"
        app:layout_constraintTop_toTopOf="@id/card_floating_timer">

        <ImageView
            android:id="@+id/iv_hint_icon"
            android:layout_width="24dp"
            android:layout_height="24dp"
            android:layout_gravity="center"
            android:src="@drawable/ic_hint_bulb"
            app:tint="@color/mint_gold" />
            
    </com.google.android.material.card.MaterialCardView>

    <!-- Floating Timer Container -->
    <com.google.android.material.card.MaterialCardView
        android:id="@+id/card_floating_timer"
        android:layout_width="72dp"
        android:layout_height="72dp"
        app:cardBackgroundColor="@color/white"
        app:cardCornerRadius="36dp"
        app:cardElevation="8dp"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintEnd_toEndOf="parent">

        <com.digitar.mintx.ui.widget.AnalogTimerView
            android:id="@+id/view_floating_timer"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:layout_margin="4dp" />
            
    </com.google.android.material.card.MaterialCardView>

</androidx.constraintlayout.widget.ConstraintLayout>
